{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --frozen-lockfile --filter @bingo/client...",
  "buildCommand": "pnpm --filter @bingo/client build",
  "outputDirectory": "packages/client/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
